{
  "name": "tasks-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "bun test packages/"
  },
  "devDependencies": {
    "@types/bun": "^1",
    "typescript": "^5",
    "@biomejs/biome": "^1"
  }
}
